(define-library (lib opengl)
   (export
      (exports (EGL version-1-1))
      (exports (OpenGL ES version-1-1))
      (exports (otus ffi)))

   (import
      (r5rs core)
      (EGL version-1-1)
      (OpenGL ES version-1-1)
      (otus ffi))

   (begin

#true

))