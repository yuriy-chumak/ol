name: linux mipsel 32/64
# notes:
#  don't forget for apt update before installing anything
#  remove man-db autoupdate, it's too time consuming

on:
  push:
    branches:
      - master

# save resources, stop outdated workflows
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  make:
    strategy:
      matrix:
        os: [ ubuntu-22.04 ]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Update submodules
      run: 'git submodule update --init --recursive'

    - name: Apt update
      run: 'sudo rm /var/lib/man-db/auto-update; sudo apt update'

    - name: Make ol/vm.h
      run: 'make includes/ol/vm.h'

    - name: Install mipsel toolchain (32- and 64-bit both)
      run: 'sudo apt install gcc-mipsel-linux-gnu gcc-mips64el-linux-gnuabi64 -y'

    - name: Install qemu mips (32- and 64-bit both)
      run: 'sudo apt install qemu-system-mipsel qemu-user'

    - name: x32 Build
      run: 'make tmp/olvm-mipsel-release'

    - name: x32 Build Info
      run: 'file tmp/olvm-mipsel-release; qemu-mipsel -L /usr/mipsel-linux-gnu tmp/olvm-mipsel-release repl --version; echo "(print (syscall 63))" |qemu-mipsel -L /usr/mipsel-linux-gnu tmp/olvm-mipsel-release repl'

    - name: x64 Build
      run: 'make tmp/olvm-mips64el-release'

    - name: x64 Build Info
      run: 'file tmp/olvm-mips64el-release; qemu-mips64el -L /usr/mips64el-linux-gnuabi64 tmp/olvm-mips64el-release repl --version; echo "(print (syscall 63))" |qemu-mips64el -L /usr/mips64el-linux-gnuabi64 tmp/olvm-mips64el-release repl'

