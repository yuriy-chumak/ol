language: c
compiler: gcc
cache: ccache

script: make tests

addons:
  apt:
    packages:
        - gcc-multilib
        - linux-libc-dev:i386

sudo: false
git:
  depth: 1
branches:
  only:
    - master
